<%= form_with(model: sport, local: true) do |form| %>
  <% if sport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sport.errors.count, "error") %> prohibited this sport from being saved:</h2>

      <ul>
      <% sport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :season_id %>
    <%= form.select :season_id, [["Summer", 1],["Winter", 2]]  %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.select :gender, [["Boy", 'Boy'],["Girl", 'Girl']] %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
