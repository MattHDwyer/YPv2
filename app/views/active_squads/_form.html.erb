<%= form_with(model: active_squad, local: true) do |form| %>
  <% if active_squad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(active_squad.errors.count, "error") %> prohibited this active_squad from being saved:</h2>

      <ul>
      <% active_squad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :team_id %>
    <%= form.text_field :team_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
